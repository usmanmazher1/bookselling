@model IEnumerable<bookselling.Models.Invoice>

@{
    ViewBag.Title = "Invoice";
    Layout = "~/Views/Shared/dashboard.cshtml";
}

<h2>Invoice</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Invoice1)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.Total_Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Bill_Date)
        </th>
       
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Invoice1)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Total_Amount)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Bill_Date)
    </td>
   
    <td>
        @Html.ActionLink("Bill Details", "Index", new { id = item.Invoice1 }) |
      
    </td>
</tr>
}

</table>
